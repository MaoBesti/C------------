9.1 编译预处理命令
    C语言提供的编译预处理命令功能主要有三类：
    1.宏定义代换功能
    2.文件包含处理功能
    3.条件选择编译功能

    编译预处理命令一般包括：
    1·#define ~#undef 宏定义或宏代换命令；
    2.#include 文件包含命令；
    3.#ifdef ~#else ~#endif
      #if ~#else ~#endif
      #ifndef ~#else ~#endif
      #if ~#elif ~#endif 等条件编译命令；
    这三种类型

    宏定义 有两种形式，即不带参数和带参数的宏定义
